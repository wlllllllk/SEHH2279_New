<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sehh2279new">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpActivity"
            android:parentActivityName=".LoginActivity" />

        <activity android:name=".LoginActivity"
            android:parentActivityName=".MainActivity" />

        <activity android:name=".SortFilterActivity" />
        <activity
            android:name=".ReviewsActivity"
            android:parentActivityName=".ProductDetailsActivity" />
        <activity
            android:name=".ShoppingRecordsActivity"
            android:parentActivityName=".AccountFragment" />
        <activity
            android:name=".ConfirmationActivity"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity
            android:name=".ProductDetailsActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ShopActivityTest"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".payment"
            android:parentActivityName=".ShopActivityTest" />
    </application>

</manifest>